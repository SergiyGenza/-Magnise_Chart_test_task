import { ToggleOnFocusDirective } from './toggle-on-focus.directive';

describe('ToggleOnFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleOnFocusDirective();
    expect(directive).toBeTruthy();
  });
});
