import { InstrumentsPipe } from './instruments.pipe';

describe('InstrumentsPipe', () => {
  it('create an instance', () => {
    const pipe = new InstrumentsPipe();
    expect(pipe).toBeTruthy();
  });
});
